export interface Permit {
  applicationNumber: string;
  applicationCategory: string;
  description: string;
  status: string;
  stageDecision: string;
  address: string;
  lodgedDate: string;  
  decisionDate: string;
}