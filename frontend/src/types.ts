export interface Permit {
  applicationNumber: string;
  applicationCategory: string;
  description: string;
  status: string;
  decisionStage: string;
  address: string;
  lodgedDate: string;  
  decisionDate: string;
}